const showMenu = () => {
    const menu = document.getElementById('menu');
    menu.classList.add('active');
};

const hideMenu = () => {
    const menu = document.getElementById('menu');
    menu.classList.remove('active');
};
